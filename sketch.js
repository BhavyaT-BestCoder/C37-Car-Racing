var ball;
var database;
var position;
var gameState = 0;
var playerCount, game, player, form;
function setup(){
    createCanvas(500,500);
   
    database= firebase.database();

    
}

function draw(){
    background("white");
    
}

